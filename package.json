{
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.921.0",
    "@aws-sdk/s3-request-presigner": "^3.921.0",
    "@playwright/test": "^1.57.0",
    "playwright": "^1.57.0",
    "supabase": "^2.63.1",
    "wrangler": "^4.45.3"
  },
  "scripts": {
    "workers:dev": "wrangler dev --remote",
    "workers:deploy": "wrangler deploy",
    "workers:deploy:dev": "wrangler deploy --env development",
    "workers:deploy:prod": "wrangler deploy --env production",
    "workers:tail": "wrangler tail",
    "workers:tail:dev": "wrangler tail --env development",
    "workers:tail:prod": "wrangler tail --env production"
  }
}
