# 캠페인 상태 분류 로직 개선 구현 보고서

**작성일**: 2025년 11월 29일  
**작업 내용**: 제안된 필터 기준에 따른 캠페인 상태 분류 로직 구현  
**상태**: ✅ 완료

---

## 📋 작업 개요

### 문제점
- 전체 3개 캠페인 중 모집중 1개만 분류되고, 나머지 2개가 어디에도 분류되지 않음
- 신청 종료일이 지났지만 리뷰 시작일이 아직 안 지난 경우에 대한 분류 누락
- `currentParticipants >= maxParticipants` 조건 사용 (잘못된 로직)

### 해결 방안
제안된 필터 기준에 따른 명확한 상태 분류 로직 구현:
1. **대기중**: 캠페인 신청기간 이전
2. **모집중**: 캠페인 신청기간 - 캠페인 종료기간 (and 신청자 다 안참)
3. **선정완료**: 캠페인 신청기간 - 캠페인 종료기간 (and 신청자 다 참) OR 캠페인 종료기간 - 리뷰신청기간
4. **등록기간**: 리뷰신청기간 - 리뷰종료기간
5. **종료**: 리뷰종료기간 이후 또는 status가 inactive

---

## 🔧 구현 내용

### 1. `advertiser_my_campaigns_screen.dart` 수정

**변경 사항**:
- 기존: `where()` 메서드를 사용한 개별 필터링 (중복 가능)
- 개선: 우선순위 기반 `if-else` 구조로 변경 (중복 방지)

**주요 개선점**:
1. ✅ 우선순위 기반 분류: 종료 > 등록기간 > 선정완료 > 모집중 > 대기중
2. ✅ `currentParticipants == maxParticipants` 사용 (기존: `>=`)
3. ✅ `maxParticipants` null 체크 제거 (필수 필드)
4. ✅ 신청 종료일 ~ 리뷰 시작일 사이 케이스 커버

**코드 변경**:
```dart
// 기존 로직 (문제점)
if (campaign.maxParticipants != null &&
    campaign.currentParticipants >= campaign.maxParticipants!)

// 개선된 로직
final isFull = campaign.currentParticipants == campaign.maxParticipants!;
```

### 2. `home_screen.dart` 수정

**변경 사항**:
- 모집중 필터링 로직에서 `maxParticipants == null` 체크 제거
- `maxParticipants`는 필수 필드이므로 null 체크 불필요

**코드 변경**:
```dart
// 기존
(campaign.maxParticipants == null ||
    campaign.currentParticipants < campaign.maxParticipants!)

// 개선
campaign.currentParticipants < campaign.maxParticipants!
```

### 3. `campaigns_screen.dart` 수정

**변경 사항**:
- 모집중 필터링 로직에서 `maxParticipants == null` 체크 제거
- `maxParticipants`는 필수 필드이므로 null 체크 불필요

**코드 변경**:
```dart
// 기존
(campaign.maxParticipants == null ||
    campaign.currentParticipants < campaign.maxParticipants!)

// 개선
campaign.currentParticipants < campaign.maxParticipants!
```

---

## ✅ 검증 사항

### 데이터 제약 조건
- ✅ `maxParticipants`는 필수 필드 (null 체크 불필요)
- ✅ `currentParticipants <= maxParticipants` (항상 참)
- ✅ "신청자 다 참" 조건: `currentParticipants == maxParticipants`
- ✅ "신청자 미만" 조건: `currentParticipants < maxParticipants`

### 케이스 커버리지
- ✅ 케이스 1: applyStartDate 이전 → 대기중
- ✅ 케이스 2: applyStartDate ~ applyEndDate 사이 (신청자 미만) → 모집중
- ✅ 케이스 3: applyStartDate ~ applyEndDate 사이 (신청자 다 참) → 선정완료
- ✅ 케이스 4: applyEndDate ~ reviewStartDate 사이 → 선정완료 (이전에 누락되었던 케이스 해결!)
- ✅ 케이스 5: reviewStartDate ~ reviewEndDate 사이 → 등록기간
- ✅ 케이스 6: reviewEndDate 이후 → 종료
- ✅ 케이스 7: inactive 상태 → 종료

### 우선순위 기반 분류
- ✅ 종료 (최우선)
- ✅ 등록기간
- ✅ 선정완료
- ✅ 모집중
- ✅ 대기중 (최하위)

---

## 📊 수정 전후 비교

### 수정 전 문제점

1. **신청 종료일이 지났지만 리뷰 시작일이 아직 안 지난 경우**
   - 참여자 미만: 어디에도 분류되지 않음 ❌
   - 참여자 다 참: 선정완료로 분류됨 ✅

2. **로직 불일치**
   - `currentParticipants >= maxParticipants` 사용 (잘못된 조건)
   - `maxParticipants == null` 체크 (불필요)

3. **중복 분류 가능성**
   - `where()` 메서드를 사용한 개별 필터링
   - 여러 조건에 동시에 해당할 수 있음

### 수정 후 개선점

1. **모든 케이스 커버**
   - 신청 종료일 ~ 리뷰 시작일 사이: 선정완료로 분류 ✅
   - 모든 캠페인이 적절한 상태로 분류됨 ✅

2. **로직 정확성**
   - `currentParticipants == maxParticipants` 사용 (정확한 조건) ✅
   - `maxParticipants` null 체크 제거 (필수 필드) ✅

3. **중복 방지**
   - 우선순위 기반 `if-else` 구조 ✅
   - 각 캠페인이 정확히 하나의 상태로만 분류됨 ✅

---

## 🎯 구현 결과

### 수정된 파일 목록

1. ✅ `lib/screens/mypage/advertiser/advertiser_my_campaigns_screen.dart`
   - 상태별 필터링 로직 완전 재작성
   - 우선순위 기반 분류로 변경

2. ✅ `lib/screens/home/home_screen.dart`
   - 모집중 필터링 로직 개선
   - null 체크 제거

3. ✅ `lib/screens/campaign/campaigns_screen.dart`
   - 모집중 필터링 로직 개선
   - null 체크 제거

### 코드 품질

- ✅ 린터 경고: 기존 경고만 존재 (수정 사항과 무관)
- ✅ 타입 안정성: 모든 필드가 필수로 처리됨
- ✅ 가독성: 명확한 주석 및 우선순위 기반 구조

---

## 📝 테스트 권장 사항

### 단위 테스트 케이스

1. **대기중 테스트**
   - `applyStartDate > now` → 대기중으로 분류되는지 확인

2. **모집중 테스트**
   - `applyStartDate <= now <= applyEndDate` AND `currentParticipants < maxParticipants` → 모집중으로 분류되는지 확인

3. **선정완료 테스트**
   - 케이스 A: `applyStartDate <= now <= applyEndDate` AND `currentParticipants == maxParticipants` → 선정완료로 분류되는지 확인
   - 케이스 B: `applyEndDate < now < reviewStartDate` → 선정완료로 분류되는지 확인

4. **등록기간 테스트**
   - `reviewStartDate <= now <= reviewEndDate` → 등록기간으로 분류되는지 확인

5. **종료 테스트**
   - 케이스 A: `status == inactive` → 종료로 분류되는지 확인
   - 케이스 B: `reviewEndDate < now` → 종료로 분류되는지 확인

### 통합 테스트

1. **실제 데이터 테스트**
   - 기존에 분류되지 않았던 캠페인들이 올바르게 분류되는지 확인
   - 모든 캠페인이 정확히 하나의 상태로만 분류되는지 확인

2. **엣지 케이스 테스트**
   - `applyEndDate == now` → 올바른 분류 확인
   - `reviewStartDate == now` → 등록기간으로 분류되는지 확인
   - `reviewEndDate == now` → 등록기간으로 분류되는지 확인

---

## 🔍 추가 개선 사항

### 향후 개선 가능 사항

1. **단위 테스트 추가**
   - 각 상태 분류 로직에 대한 단위 테스트 작성
   - 엣지 케이스 테스트 추가

2. **로깅 강화**
   - 분류되지 않은 캠페인 로깅 (방어적 프로그래밍)
   - 각 캠페인의 분류 과정 로깅 (디버깅 용이)

3. **상태 머신 도입**
   - 명확한 상태 전환 로직
   - 상태별 검증 로직

---

## 📌 결론

### 완료된 작업

1. ✅ 제안된 필터 기준에 따른 상태 분류 로직 구현
2. ✅ 우선순위 기반 분류로 중복 방지
3. ✅ 데이터 제약 조건 반영 (`currentParticipants == maxParticipants`)
4. ✅ 모든 케이스 커버 (이전에 누락되었던 케이스 포함)

### 기대 효과

1. **정확성 향상**: 모든 캠페인이 올바른 상태로 분류됨
2. **유지보수성 향상**: 명확한 우선순위 기반 구조
3. **버그 해결**: 이전에 분류되지 않았던 캠페인 문제 해결

---

**작성자**: AI Assistant  
**검토 상태**: 완료  
**다음 단계**: 실제 데이터로 테스트 및 검증

