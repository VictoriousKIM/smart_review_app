---
alwaysApply: true
---

## 데이터베이스 덤프 (시드 데이터)

### ✅ 올바른 방법 (로컬)
```bash
npx supabase db dump --local --data-only -f supabase/seed.sql
```

### ❌ 잘못된 방법 (원격/프로덕션)
```bash
npx supabase db dump --data-only -f supabase/seed.sql
```

### 마이그레이션 파일 압축
```bash
npx supabase migration squash
```

### 로컬대시보드 변경사항 마이그레이션 적용
```bash
npx supabase db diff -f <migration_name>
```

### 스키마 변경 마이그레이션 추가
```bash
# 현재 DB와 마이그레이션 파일 차이점 확인 후 새 마이그레이션 생성
npx supabase db diff --local --schema public -f supabase/migrations/YYYYMMDDHHMMSS_description.sql
```

**중요:**
- `--local` 플래그가 없으면 기본적으로 원격(프로덕션) DB에서 덤프를 가져옵니다
- 로컬 개발 환경과 프로덕션 환경은 데이터 구조가 다를 수 있습니다
- `seed.sql`은 로컬 개발용 테스트 데이터여야 합니다
- 원격 DB에서 덤프를 가져오면 로컬 환경과 맞지 않아 리셋이 실패할 수 있습니다

## Git 커밋 메시지 작성 규칙

### ✅ 올바른 방법 (영어로 작성)
```bash
git commit -m "feat: add point cancellation feature

- Add cancel cash transaction RPC function
- Update UI to show cancel button for pending transactions
- Remove reviewer charge functionality"
```

### ❌ 잘못된 방법 (한글로 작성)
```bash
git commit -m "feat: 포인트 취소 기능 추가"
```

**중요:**
- 모든 커밋 메시지는 **영어로 작성**해야 합니다
- 한글 커밋 메시지는 Windows PowerShell에서 깨져 보일 수 있습니다
- GitHub에서는 정상 표시되지만, 로컬 터미널에서 가독성이 떨어집니다
- 커밋 타입: `feat`, `fix`, `chore`, `refactor`, `docs`, `style`, `test` 등

## 📱 Flutter 웹 앱 접근성 활성화

Flutter 웹 앱에서 접근성 버튼을 활성화하는 방법:

```javascript
// 접근성 활성화 (필수!)
await page.evaluate(() => {
  const accessibilityButton = document.querySelector('flt-semantics-placeholder[aria-label="Enable accessibility"]');
  if (accessibilityButton) {
    accessibilityButton.click();
  }
});
```

## 📄 문서 작성 규칙

### 보고서 작성 시 날짜 반영

### ✅ 올바른 방법 (현재 날짜 사용)
보고서나 문서를 작성할 때는 **반드시 현재 날짜를 확인**하고 사용해야 합니다:

```bash
# PowerShell에서 현재 날짜 확인
powershell -Command "Get-Date -Format 'yyyy년 MM월 dd일'"
```

보고서 작성 예시:
```markdown
**작성일**: 2025년 11월 24일  
**작업 기간**: 2025년 11월 24일
```

### ❌ 잘못된 방법 (임의의 날짜 사용)
```markdown
**작성일**: 2024년 12월 25일  # ❌ 임의의 날짜 사용
```

**중요:**
- 보고서나 문서 작성 시 **항상 현재 날짜를 확인**한 후 사용해야 합니다
- `run_terminal_cmd` 도구를 사용하여 현재 날짜를 확인할 수 있습니다
- 날짜 형식: `yyyy년 MM월 dd일` (예: 2025년 11월 24일)
- 마이그레이션 파일명의 타임스탬프는 생성 시점을 나타내므로 변경하지 않아도 됩니다

## 🖼️ Flutter 웹 앱 이미지 업로드 방법

Flutter 웹 앱에서 이미지를 업로드하는 방법:

### ✅ 올바른 방법

1. **이미지 파일을 웹 서버에 복사**:
```bash
# Flutter 웹 앱이 localhost:3001에서 실행 중인 경우
powershell -Command "Copy-Item '이미지파일.png' -Destination 'web/' -Force"
```

2. **"이미지 선택" 버튼 클릭 후 file input에 이미지 설정**:
```javascript
// 이미지 선택 버튼 클릭
await page.locator('button:has-text("이미지 선택")').click();
await page.waitForTimeout(1000);

// fetch로 이미지 가져와서 File 객체 생성
const response = await fetch('http://localhost:3001/이미지파일.png');
const blob = await response.blob();
const file = new File([blob], '이미지파일.png', { type: 'image/png' });

// file input 찾아서 DataTransfer로 파일 설정
const fileInput = document.querySelector('input[type="file"]');
const dataTransfer = new DataTransfer();
dataTransfer.items.add(file);
fileInput.files = dataTransfer.files;

// change 이벤트 발생
const changeEvent = new Event('change', { bubbles: true });
fileInput.dispatchEvent(changeEvent);
```

**중요:**
- "이미지 선택" 버튼을 클릭하면 file input이 나타남
- `DataTransfer` API를 사용하여 파일을 설정해야 함
- 이미지 파일은 Flutter 웹 앱과 같은 도메인(localhost:3001)에서 제공되어야 함

## ⏰ DateTime(일시) 처리 규칙

**핵심 원칙:**
- 모든 DateTime은 **한국 시간(KST, UTC+9)** 기준으로 처리
- DB에는 UTC로 저장하되, 애플리케이션에서는 항상 한국 시간으로 변환하여 사용

**1. DateTime 생성**
```dart
import '../../utils/date_time_utils.dart';

final dateTime = DateTimeUtils.nowKST().copyWith(
  year: date.year,
  month: date.month,
  day: date.day,
  hour: time.hour,
  minute: time.minute,
  second: 0,
  millisecond: 0,
);
```

**2. 현재 시간 사용**
```dart
final now = DateTimeUtils.nowKST();
```

**3. DB에 저장**
```dart
import '../utils/date_time_utils.dart';

'p_apply_start_date': DateTimeUtils.toIso8601StringKST(applyStartDate),
```

**4. DB에서 읽기**
```dart
import '../utils/date_time_utils.dart';

applyStartDate: json['apply_start_date'] != null
    ? DateTimeUtils.parseKST(json['apply_start_date'])
    : DateTimeUtils.nowKST().add(const Duration(days: 1)),
```

**5. 시간 비교**
```dart
import '../../../utils/date_time_utils.dart';

void _updateFilteredCampaigns() {
  final now = DateTimeUtils.nowKST();
  _pendingCampaigns = _allCampaigns.where((campaign) {
    return campaign.applyStartDate.isAfter(now);
  }).toList();
}
```

**6. 날짜/시간 표시**
```dart
import '../../../utils/date_time_utils.dart';

Text(DateTimeUtils.formatKST(dateTime)) // "2025-11-25 14:30"
```

**금지 사항:**
- ❌ `DateTime.now()` 직접 사용 금지 → `DateTimeUtils.nowKST()` 사용
- ❌ `.toLocal()` 사용 금지 → `DateTimeUtils.parseKST()` 사용
- ❌ `.toIso8601String()` 직접 사용 금지 → `DateTimeUtils.toIso8601StringKST()` 사용